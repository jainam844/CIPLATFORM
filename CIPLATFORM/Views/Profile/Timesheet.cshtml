@* @using CIPLATFORM.Entities.ViewModels
@model ProfileViewModel

<style>

    table {
        display: block;
        overflow-x: auto;
        white-space: nowrap;
    }

        table tbody {
            display: table;
            width: 100%;
        }

    table, tr, td, th {
        padding: 10px;
        margin: auto;
        border: none;
    }

    .profile-nav {
        width: 40px;
        height: 40px;
        border-radius: 50%;
    }

    .nav-item {
        list-style-type: None;
    }

    .profile {
        width: 40px;
        height: 40px;
        border-radius: 50%;
    }
</style>



<div class="container">
    <h2 class="mb-4 mt-4">Volunteering Timesheet</h2>
    <div class="row">
        <div class="col-lg-6">
            <div class="container">
                <div class="border">
                    <div class="container">
                        <div class="row mt-4">
                            <div class="col-6">
                                <h6>Volunteering Hours</h6>
                            </div>
                            <div class="col-6 d-flex justify-content-end">



                                <button type="button" class="btn btn-outline-warning rounded-pill" onclick="getActivity();" data-bs-target="#TimesheetTime"
                                        data-bs-toggle="modal" >
                                    <i class="bi bi-plus"></i>
                                    Add
                                </button>


                                <div class="modal fade" id="exampleModal1" tabindex="-1"
                                     aria-labelledby="exampleModalLabel" aria-hidden="true">

                                    @{
                                        Html.RenderPartial("_TimeCard");

                                    }
                                  
                                </div>

                            </div>

                        </div>
                        <table class="table ">
                            <tbody>
                                <tr class=" mt-3 mb-3">

                                    <th scope="col"><b>Mission</b> </th>
                                    <th scope="col"><b>Date</b> </th>
                                    <th scope="col"><b>Hours</b> </th>
                                    <th scope="col"> <b>Minutes</b> </th>
                                    <th scope="col"></th>
                                </tr>

                                @foreach (var obj in Model.timesheets)
                                {
                                    <tr class=" mt-3 mb-3">
                                        <td scope="col">@obj.Mission.Title</td>
                                        <td scope="col">@obj.DateVolunteereed.ToString().Substring(0,10)</td>
                                        <td scope="col">@obj.Time.ToString().Substring(0,2)h</td>
                                        <td scope="col">@obj.Time.ToString().Substring(3,2)min</td>
                                        <td scope="col">
                                            <button type="button" class="btn p-0" data-bs-toggle="modal" data-bs-target="#exampleModal1">
                                                <i class="bi bi-pencil-square" style="color: coral;"></i>
                                            </button>
                                            <button type="button" class="btn p-0" data-bs-toggle="modal" data-bs-target="#exampleModal1">
                                                <i class="bi bi-trash ms-3"></i>
                                            </button>
                                        </td>

                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>


        <div class="col-lg-6 mt-lg-0 mt-3">
            <div class="container">
                <div class="border">
                    <div class="container">
                        <div class="row mt-4">
                            <div class="col-6">
                                <h6>Volunteering Goals</h6>
                            </div>
                            <div class="col-6 d-flex justify-content-end">
                                <button type="button" class="btn btn-outline-warning rounded-pill"
                                        data-bs-toggle="modal" data-bs-target="#exampleModal">
                                    <i class="bi bi-plus"></i>
                                    Add
                                </button>


                                <div class="modal fade" id="exampleModal" tabindex="-1"
                                     aria-labelledby="exampleModalLabel" aria-hidden="true">

                                    @{
                                        Html.RenderPartial("_GoalCard");

                                    }
                                  
                                </div>


                            </div>

                        </div>
                        <table class="table ">
                            <tbody>
                                <tr class="  mt-3 mb-3">
                                    <th scope="col"><b>Mission</b> </th>
                                    <th scope="col"> <b>Date</b> </th>
                                    <th scope="col"> <b>Action</b> </th>

                                    <th scope="col"></th>

                                </tr>

                                @foreach (var obj in Model.goaltimesheets)
                                {
                                    <tr class=" mt-3 mb-3">
                                        <td scope="col">@obj.Mission.Title</td>
                                        <td scope="col">@obj.DateVolunteereed.ToString().Substring(0,10)</td>
                                        <td scope="col">@obj.Action</td>

                                        <td scope="col">
                                            <button type="button" class="btn p-0" data-bs-toggle="modal"
                                                data-bs-target="#exampleModal">
                                                <i class="bi bi-pencil-square" style="color: coral;"></i>
                                            </button>
                                            <button type="button" class="btn p-0" data-bs-toggle="modal"
                                                data-bs-target="#exampleModal">
                                                <i class="bi bi-trash ms-3"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


*@










@using CIPLATFORM.Entities.ViewModels
@model ProfileViewModel



<style>

    table {
        display: block;
        overflow-x: auto;
        white-space: nowrap;
    }

        table tbody {
            display: table;
            width: 100%;
        }

    table, tr, td, th {
        padding: 10px;
        margin: auto;
        border: none;
    }

    .profile-nav {
        width: 40px;
        height: 40px;
        border-radius: 50%;
    }

    .nav-item {
        list-style-type: None;
    }

    .profile {
        width: 40px;
        height: 40px;
        border-radius: 50%;
    }
</style>

<hr />
<div class="container">
    <h2 class="mb-4 mt-4">Volunteering Timesheet</h2>
    <div class="row">
         @if (TempData["true"] != null)
            {
                <script type="text/javascript">
                    toastr.success('@TempData["true"]')
                </script>
            }
            @if (TempData["false"] != null)
            {
                <script type="text/javascript">
                    toastr.success('@TempData["false"]')
                </script>
            }
        <div class="col-lg-6">
            <div class="container">
                <div class="border">
                    <div class="container">
                        <div class="row mt-4">
                            <div class="col-6">
                                <h6>Volunteering Hours</h6>
                            </div>
                            <div class="col-6 d-flex justify-content-end">
                                <button type="button" class="btn btn-outline-warning rounded-pill" data-bs-toggle="modal" onclick="getActivity();" data-bs-target="#TimesheetTime">
                                    <i class="bi bi-plus"></i>
                                    Add
                                </button>
                                <div class="modal fade" id="TimesheetTime" tabindex="-1" aria-labelledby="exampleModalLabel"  aria-hidden="true">
                                    @{
                                        Html.RenderPartial("_TimeCard");
                                    }
                                </div>
                            </div>
                        </div>
                        <table class="table ">
                            <tbody>
                                <tr class=" mt-3 mb-3">
                                    <th scope="col"><b>Mission</b> </th>
                                    <th scope="col"><b>Date</b> </th>
                                    <th scope="col"><b>Hours</b> </th>
                                    <th scope="col"><b>Minutes</b> </th>
                                    <th scope="col"></th>
                                </tr>
                                @foreach (var obj in Model.timesheets)
                                {
                                    <tr class=" mt-3 mb-3">
                                        <td scope="col">@obj.Mission.Title</td>
                                        <td scope="col">@obj.DateVolunteereed.ToString().Substring(0,10)</td>
                                        <td scope="col">@obj.Time.Value.Hours h</td>
                                        <td scope="col">@obj.Time.Value.Minutes min</td>
                                        <td scope="col">
                                            <button type="button" class="btn p-0" data-bs-toggle="modal" data-bs-target="#TimesheetTime" onclick="getActivity(@obj.TimesheetId);">
                                                <i class="bi bi-pencil-square" style="color: coral;"></i>
                                            </button>
                                           
                                            <button type="button" class="btn p-0" data-bs-toggle="modal" data-bs-target="#TimesheetTime">
                                                <i class="bi bi-trash ms-3"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-6 mt-lg-0 mt-3">
            <div class="container">
                <div class="border">
                    <div class="container">
                        <div class="row mt-4">
                            <div class="col-6">
                                <h6>Volunteering Goals</h6>
                            </div>
                            <div class="col-6 d-flex justify-content-end">
                                <button type="button" class="btn btn-outline-warning rounded-pill" onclick="getgoalActivity()" data-bs-toggle="modal" data-bs-target="#TimesheetGoal">
                                    <i class="bi bi-plus"></i>
                                    Add
                                </button>
                                <div class="modal fade" id="TimesheetGoal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                    @{
                                        Html.RenderPartial("_GoalCard");
                                    }
                                    
                                </div>
                            </div>
                        </div>
                        <table class="table ">
                            <tbody>
                                <tr class=" mt-3 mb-3">
                                    <th scope="col"><b>Mission</b></th>
                                    <th scope="col"><b>Date</b></th>
                                    <th scope="col"><b>Action</b></th>
                                    <th scope="col"></th>
                                </tr>
                                @foreach (var obj in Model.goaltimesheets)
                                {
                                    <tr class=" mt-3 mb-3">
                                        <td scope="col">@obj.Mission.Title</td>
                                        <td scope="col">@obj.DateVolunteereed.ToString().Substring(0,10)</td>
                                        <td scope="col">@obj.Action</td>
                                        <td scope="col">
                                            <button type="button" class="btn p-0" data-bs-toggle="modal" data-bs-target="#TimesheetGoal" onclick="getgoalActivity(@obj.TimesheetId)">
                                                <i class="bi bi-pencil-square" style="color: coral;"></i>
                                            </button>
                                            <button type="button" class="btn p-0" data-bs-toggle="modal" data-bs-target="#TimesheetGoal">
                                                <i class="bi bi-trash ms-3"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
